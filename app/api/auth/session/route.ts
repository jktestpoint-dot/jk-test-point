import { NextRequest, NextResponse } from "next/server";
import { ACCESS_TOKEN_COOKIE, REFRESH_TOKEN_COOKIE } from "@/lib/supabase-auth";
export async function POST(request: NextRequest) { try { const { access_token, refresh_token } = await request.json(); if (typeof access_token !== "string" || typeof refresh_token !== "string") return NextResponse.json({ error: "Invalid session." }, { status: 400 }); const response = NextResponse.json({ ok: true }); const options = { httpOnly: true, sameSite: "lax" as const, secure: process.env.NODE_ENV === "production", path: "/", maxAge: 60 * 60 * 24 * 7 }; response.cookies.set(ACCESS_TOKEN_COOKIE, access_token, options); response.cookies.set(REFRESH_TOKEN_COOKIE, refresh_token, options); return response; } catch { return NextResponse.json({ error: "Invalid session request." }, { status: 400 }); } }
