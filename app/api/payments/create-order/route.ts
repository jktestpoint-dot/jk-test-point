import {NextRequest} from "next/server"; export async function POST(request:NextRequest){const {amount,testId}=await request.json();if(!amount||!testId)return Response.json({error:"amount and testId are required"},{status:400});if(!process.env.RAZORPAY_KEY_ID||!process.env.RAZORPAY_KEY_SECRET)return Response.json({error:"Payments are not configured yet"},{status:503});return Response.json({message:"Connect Razorpay SDK here",amount,testId})}
