"use client";
import { FormEvent, useState } from "react";
import Link from "next/link";
export default function ResetPassword(){const [message,setMessage]=useState("");const submit=async(e:FormEvent<HTMLFormElement>)=>{e.preventDefault();const password=String(new FormData(e.currentTarget).get("password")||"");const hash=new URLSearchParams(window.location.hash.slice(1));const accessToken=hash.get("access_token");const r=await fetch("/api/auth/reset-password",{method:"POST",headers:{"Content-Type":"application/json"},body:JSON.stringify({accessToken,password})});const x=await r.json();setMessage(r.ok?"Password updated. You can now log in.":x.error)};return <section className="container-page py-14"><form onSubmit={submit} className="card mx-auto max-w-md"><h1 className="text-3xl font-bold">Set a new password</h1><label className="mt-6 block text-sm font-medium">New password<input className="input mt-2" name="password" type="password" minLength={8} required/></label>{message&&<p className="mt-4 text-sm">{message}</p>}<button className="btn-primary mt-6 w-full">Update password</button><Link className="mt-4 block text-center text-sm" href="/login">Back to login</Link></form></section>}
